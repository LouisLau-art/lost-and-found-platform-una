export default defineAppConfig({
    una: {
        // Primary color - used for buttons, links, etc.
        primary: 'yellow',
        // Gray color palette for background, borders, text
        gray: 'stone',
        // Border radius (in rem)
        radius: 0.5,
        // Base font size (in px)
        fontSize: 16
    }
})
